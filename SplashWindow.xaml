<Window x:Class="WpfMediaPlayerRA.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True"
        Width="294" Height="190" Background="White"
        AllowsTransparency="False">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="Black"/>
                <GradientStop Color="White" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Image Source="/Assets/Images/player_play_vert(128).png" Width="109" Height="116" RenderTransformOrigin="0.5,0.5" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,74,0,0">
            <Image.RenderTransform>
                <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1"/>
            </Image.RenderTransform>
            <Image.Triggers>
                <!-- Animation pulsation -->
                <EventTrigger RoutedEvent="Image.Loaded">
                    <BeginStoryboard>
                        <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                            <DoubleAnimation Storyboard.TargetName="scaleTransform"
                                         Storyboard.TargetProperty="ScaleX"
                                         From="1" To="1.2" Duration="0:0:1"/>
                            <DoubleAnimation Storyboard.TargetName="scaleTransform"
                                         Storyboard.TargetProperty="ScaleY"
                                         From="1" To="1.2" Duration="0:0:1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Image.Triggers>
        </Image>

        <TextBlock Text=" V1.3" FontSize="8" Foreground="White" Margin="0,0,0,25"/>
        <TextBlock x:Name="txtChargement"  Text="Chargement" FontSize="28" Foreground="White"
                   VerticalAlignment="Top" Margin="63,15,33,0" Height="44"/>
    </Grid>
</Window>

