<Window x:Class="WpfMediaPlayerRA.SplashWindowOld"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Width="520" Height="240"
        Background="#FFF"
        AllowsTransparency="False">

    <Border CornerRadius="12" BorderBrush="#DDD" BorderThickness="1" Padding="20" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Chargement..."
                       FontSize="22" FontWeight="Bold" Foreground="#333"
                       HorizontalAlignment="Center" Margin="0,0,0,12"/>
            <TextBlock Text="{Binding Status}"
                       FontSize="16" Foreground="#555"
                       HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <ProgressBar Height="18" Width="420"
                         IsIndeterminate="{Binding IsIndeterminate}"
                         Minimum="0" Maximum="100"
                         Value="{Binding ProgressPercent}" Margin="29,49,29,-67" Grid.Row="3"/>
            <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0}%}"
                       HorizontalAlignment="Center" Margin="0,8,0,0" Foreground="#777"/>
        </Grid>
    </Border>
</Window>
